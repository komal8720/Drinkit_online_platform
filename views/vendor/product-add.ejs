<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= title %></title>

    <!-- Bootstrap CSS -->
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet">
    <!-- Bootstrap Icons -->
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.3/font/bootstrap-icons.min.css">
    <!-- Inter Font -->
    <link href="https://fonts.googleapis.com/css2?family=Inter:wght@400;500;600;700;800&display=swap" rel="stylesheet">
    <!-- Custom Admin / Vendor CSS -->
    <link rel="stylesheet" href="/css/admin-layout.css">
</head>

<body>
    <!-- SIDEBAR -->
    <%- include("partials/sidebar", { currentPage: "product-add" }) %>

    <!-- MAIN -->
    <main class="admin-main" id="adminMain">
        <!-- NAVBAR -->
        <%- include("partials/navbar") %>

        <!-- CONTENT -->
        <div class="admin-content">
            <!-- BREADCRUMB -->
            <div class="admin-breadcrumb">
                <a href="/vendor/dashboard">Vendor Portal</a>
                <i class="bi bi-chevron-right"></i>
                <a href="/vendor/products">Products</a>
                <i class="bi bi-chevron-right"></i>
                <strong>Add New Beverage</strong>
            </div>

            <!-- FLASH NOTIFICATIONS -->
            <% if (typeof error_msg !== 'undefined' && error_msg && error_msg.length > 0) { %>
                <div class="alert alert-danger alert-dismissible fade show" role="alert">
                    <i class="bi bi-exclamation-triangle-fill me-2"></i><%= error_msg %>
                    <button type="button" class="btn-close" data-bs-dismiss="alert" aria-label="Close"></button>
                </div>
            <% } %>

            <!-- HEADER -->
            <div class="d-flex justify-content-between align-items-center mb-4">
                <div>
                    <h1 class="fw-bold mb-1" style="font-size: 1.75rem; letter-spacing: -0.5px;">Add New Beverage</h1>
                    <p class="text-muted mb-0 small">Create a new item in your store inventory</p>
                </div>
                <div>
                    <a href="/vendor/products" class="btn btn-outline-secondary">
                        <i class="bi bi-arrow-left me-1"></i> Back to Products
                    </a>
                </div>
            </div>

            <!-- FORM -->
            <form action="/vendor/products/add?_csrf=<%= csrfToken %>" method="POST" enctype="multipart/form-data" class="needs-validation" novalidate>
                <input type="hidden" name="_csrf" value="<%= csrfToken %>">

                <div class="row g-4">
                    <!-- PRIMARY DETAILS -->
                    <div class="col-lg-8">
                        <div class="dashboard-card mb-4">
                            <div class="dashboard-card-header">
                                <span class="dashboard-card-title">Beverage Information</span>
                            </div>
                            <div class="dashboard-card-body p-4">
                                <div class="mb-3">
                                    <label class="form-label fw-semibold">Product Name <span class="text-danger">*</span></label>
                                    <input type="text" name="name" class="form-control" placeholder="e.g. Royal Reserve Blended Malt" required maxlength="200">
                                </div>

                                <div class="row g-3 mb-3">
                                    <div class="col-md-6">
                                        <label class="form-label fw-semibold">Category <span class="text-danger">*</span></label>
                                        <select name="category_id" class="form-select" required>
                                            <option value="">Select Category</option>
                                            <% categories.forEach(c => { %>
                                                <option value="<%= c.id %>"><%= c.name %></option>
                                            <% }) %>
                                        </select>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label fw-semibold">SKU (Stock Keeping Unit)</label>
                                        <input type="text" name="sku" class="form-control" placeholder="Leave empty to auto-generate">
                                    </div>
                                </div>

                                <div class="mb-3">
                                    <label class="form-label fw-semibold">Product Description</label>
                                    <textarea name="description" class="form-control" rows="4" placeholder="Detailed tasting notes, ingredients, origin, etc."></textarea>
                                </div>

                                <div class="row g-3 mb-3">
                                    <div class="col-md-6">
                                        <label class="form-label fw-semibold">Bottle / Pack Size</label>
                                        <input type="text" name="bottle_size" class="form-control" placeholder="e.g. 750ml, 650ml, 330ml Can">
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label fw-semibold">Alcohol Percentage (ABV %)</label>
                                        <div class="input-group">
                                            <input type="number" step="0.1" min="0" max="100" name="alcohol_percentage" class="form-control" placeholder="e.g. 42.8">
                                            <span class="input-group-text">%</span>
                                        </div>
                                    </div>
                                </div>
                            </div>
                        </div>

                        <!-- PRICING & INVENTORY -->
                        <div class="dashboard-card">
                            <div class="dashboard-card-header">
                                <span class="dashboard-card-title">Pricing &amp; Inventory</span>
                            </div>
                            <div class="dashboard-card-body p-4">
                                <div class="row g-3 mb-3">
                                    <div class="col-md-6">
                                        <label class="form-label fw-semibold">Regular Price (₹) <span class="text-danger">*</span></label>
                                        <div class="input-group">
                                            <span class="input-group-text">₹</span>
                                            <input type="number" step="0.01" min="1" name="price" class="form-control" required placeholder="0.00">
                                        </div>
                                    </div>
                                    <div class="col-md-6">
                                        <label class="form-label fw-semibold">Discounted Sale Price (₹)</label>
                                        <div class="input-group">
                                            <span class="input-group-text">₹</span>
                                            <input type="number" step="0.01" min="0" name="sale_price" class="form-control" placeholder="Optional">
                                        </div>
                                    </div>
                                </div>

                                <div class="row g-3 mb-3">
                                    <div class="col-md-4">
                                        <label class="form-label fw-semibold">Stock Quantity <span class="text-danger">*</span></label>
                                        <input type="number" min="0" name="stock_quantity" class="form-control" value="10" required>
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label fw-semibold">Min Order Qty</label>
                                        <input type="number" min="1" name="min_order_quantity" class="form-control" value="1">
                                    </div>
                                    <div class="col-md-4">
                                        <label class="form-label fw-semibold">Max Order Qty</label>
                                        <input type="number" min="1" name="max_order_quantity" class="form-control" value="10">
                                    </div>
                                </div>
                            </div>
                        </div>
                    </div>

                    <!-- SIDE CONTROLS -->
                    <div class="col-lg-4">
                        <div class="dashboard-card mb-4">
                            <div class="dashboard-card-header">
                                <span class="dashboard-card-title">Product Image</span>
                            </div>
                            <div class="dashboard-card-body p-4">
                                <div class="mb-3">
                                    <label class="form-label fw-semibold">Bottle / Pack Image</label>
                                    <input type="file" name="image" class="form-control" accept="image/jpeg,image/png,image/webp">
                                    <div class="form-text">JPG, PNG, or WEBP. Max 5MB.</div>
                                </div>
                            </div>
                        </div>

                        <div class="dashboard-card">
                            <div class="dashboard-card-header">
                                <span class="dashboard-card-title">Settings &amp; Restrictions</span>
                            </div>
                            <div class="dashboard-card-body p-4">
                                <div class="form-check mb-3">
                                    <input class="form-check-input" type="checkbox" name="is_age_restricted" value="1" id="is_age_restricted" checked>
                                    <label class="form-check-label fw-semibold" for="is_age_restricted">
                                        21+ Age Restricted Item
                                    </label>
                                    <div class="form-text small">Mandates age verification before customer checkout.</div>
                                </div>

                                <div class="form-check mb-4">
                                    <input class="form-check-input" type="checkbox" name="is_featured" value="1" id="is_featured">
                                    <label class="form-check-label fw-semibold" for="is_featured">
                                        Feature on Store Front
                                    </label>
                                </div>

                                <button type="submit" class="btn btn-primary w-100 py-2" style="background-color: var(--primary); border-color: var(--primary);">
                                    <i class="bi bi-cloud-arrow-up me-1"></i> Save Product
                                </button>
                            </div>
                        </div>
                    </div>
                </div>
            </form>
        </div>

        <!-- FOOTER -->
        <%- include("partials/footer") %>
    </main>

    <!-- Bootstrap JS -->
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>
